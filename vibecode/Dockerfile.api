FROM node:20-alpine

WORKDIR /app

# Copy API package files
COPY apps/api/package.json apps/api/package-lock.json* ./

# Install dependencies
RUN npm install

# Copy API source
COPY apps/api/ .

# Build TypeScript
RUN npm run build

# Expose port
EXPOSE 4000

# Start the server
CMD ["npm", "start"]
